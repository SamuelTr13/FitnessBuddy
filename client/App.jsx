import React, {Component} from 'react';

// import MainContainer from './containers/MainContainer';

class App extends Component {
  //keep track of a initial time
  //when function is called, if current time subtracting, call setState

  render() {
    return (
      //rendered to the page
      <div>
        <h1>Tic Tac Toe</h1>
      </div>
    );
  }
}

export default App;